Form Versions
=============

Every save creates a new form version. The versions screen
(``@@form-versions``) lists them and lets you preview, restore, lock,
download or upload versions (see :doc:`endpoints` for the underlying
operations).

.. image:: _static/screenshots/survey-form-versions.png
   :align: center
   :alt: Form versions
